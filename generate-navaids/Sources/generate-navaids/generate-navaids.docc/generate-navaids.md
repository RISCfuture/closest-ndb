# ``generate_navaids``

@Metadata {
  @DisplayName("generate-navaids")
}

Generates the `ndbs.json` file from FAA NASR data.

## Overview

Run the tool with `swift run` and copy the generated `ndbs.json` file to the
appropriate place. The current NASR distribution is downloaded if it does not
already exist on disk.
